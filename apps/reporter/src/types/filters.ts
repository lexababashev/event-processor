export interface EventFilter {
    from?: string;
    to?: string;
    source?: string;
    funnelStage?: string;
    eventType?: string;
  }
  
  export interface RevenueFilter {
    from?: string;
    to?: string;
    source?: string;
    campaignId?: string;
  }
  
  export interface DemographicsFilter {
    from?: string;
    to?: string;
    source?: string;
  }
  